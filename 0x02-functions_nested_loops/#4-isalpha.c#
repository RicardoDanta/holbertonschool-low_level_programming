#include "main.h"
/**
 * _isalpha - a function that checks for alphabetic character
 * @c: isalpha
 * Return: 1 if is a letter, 0 if is otherwise
 */
int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z')
	else if (c >= 'A' && c <= 'Z')

		return (1);
	else
		return (0);
}