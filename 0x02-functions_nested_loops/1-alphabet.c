#include <stdio.h>
#include "main.h"
/**
 * main - main.c
 *
 * Return: Always 0
 */
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		_putchar(low);
	void print_alphabet(void);
	_putchar('\n');

	return (0);
}
