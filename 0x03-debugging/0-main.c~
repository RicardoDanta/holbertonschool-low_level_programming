#include <stdio.h>
#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 * Return: 0
 */

int main(void)
{
	int i;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	for (i = 0)
		printf("%d is zero\n", i);
	positive_or_negative(i);

	return (0);
}
