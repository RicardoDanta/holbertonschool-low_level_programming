#include "main.h"

/**
 * _strlen - a function that prints a string
 * @str: string to print
 * Return: Always 0
 */
int _strlen(char *s)
{
	char c;
	c = *s;
	int i = 0;

	while (c != '\0')
	{
		i++;
		c = *(s + i);
	}
	return (i);
}
